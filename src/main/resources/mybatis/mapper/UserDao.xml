<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="app.foodylab.adaptor.infra.mapper.UserMapper">

  <insert id="userInsert" parameterType="app.foodylab.domain.user.User">
    INSERT INTO user
      (name)
    VALUES
      (#{name});
  </insert>
</mapper>